# LLM-Rosetta Go Rewrite Gap Audit

## Scope and baseline

- sub2api baseline: `main@dc6ae080`
- implementation branch: `feature/llm-rosetta-go-rewrite`
- reference repository: `../llm-rosetta-reference`
- pinned reference commit: `81ed15999e9a9c40082a7392bd0c0366c18960a0`
- audited protocols: OpenAI Chat Completions, OpenAI Responses, Anthropic Messages, Google GenAI

This audit tracks behavioral responsibilities, not file-for-file translation. sub2api keeps account selection, authentication, retries, sticky sessions, provider policy, usage accounting, and billing in its existing service domains. The protocol conversion package owns only standard wire semantics and request-scoped conversion state.

Status values used below:

- **integrated**: implemented and used by all applicable production paths.
- **isolated**: implemented in Go and tested, but not consistently used by production paths.
- **partial**: only part of the reference behavior or route matrix is implemented.
- **legacy duplicate**: production behavior exists in service-local or pairwise code outside the common core.
- **missing**: no applicable Go implementation.
- **excluded**: intentionally not part of this rewrite, with the reason recorded.

## Responsibility ledger

| Reference responsibility | Reference files and tests | Current Go implementation | Production entrypoints using it | Status | Known difference and required action | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| Explicit protocol IDs | `auto_detect.py`, `routing.py`, `tests/test_auto_detect.py` | `internal/pkg/protocolconv/protocol.go`, `Route`, and structured result `ActualProtocol` | All ordinary standard generation services construct explicit source/intended-target routes and pass actual wire protocol into response/stream conversion | integrated | Route handlers define source protocol; scheduler/account capability defines intended target; provider attempt results define actual protocol. Core endpoint/model/body inference is deliberately excluded. | Protocol/route validation, actual-protocol propagation, fallback tests |
| Converter registry | `converters/__init__.py`, `tests/test_public_api.py` | One `protocolconv.Registry` initialized by `standard.NewRegistry` | All applicable ordinary Chat, Responses, Messages, and Google generation paths; vendor adapters wrap standard bodies outside it | integrated | Antigravity vendor envelopes, compact, image/media, and WebSocket session mechanics are explicit non-standard boundaries, not alternate registries. | Registry tests; 4x4 request/response/stream matrices; production integrations |
| One-shot conversion pipeline | `pipeline.py`, `tests/test_pipeline.py`, `tests/test_pipeline_profile.py` | `protocolconv.Pipeline` binds explicit protocols, model identities, warnings, response conversion, stream creation, tool routes, and scoped metadata | Every ordinary standard generation attempt across OpenAI, generic Anthropic/Bedrock, Gemini, OpenCode Go, and Antigravity OAuth Chat/Responses creates an attempt-local Pipeline and retains the successful attempt through output; Codex/provider transforms and Antigravity `v1internal` wrapping run after request conversion | integrated | Retries that rebuild/send a request create fresh state. Vendor envelopes and service retry/billing/timeout policy remain outside. Specialized exclusions are listed below. | Lifecycle, actual-protocol, byte-preservation, retry isolation, provider integration, race tests |
| Request to IR to target request | `pipeline.py`, four converter suites | Four Go converters and common IR; identity requests validate and preserve bytes | Complete production 4x4 standard generation matrix, including raw-Chat and Chat fallback, native Responses, generic Anthropic/Bedrock, native Gemini, OpenCode Go, and Messages-to-Responses Codex policy | integrated | Pairwise production conversion remains only for Cursor response-only endpoint compatibility and non-generation `countTokens`. Vertex/Code Assist/Bedrock outer transport envelopes stay provider-owned. | 4x4 core matrices and production cell tests below |
| Target response to IR to source response | `pipeline.py`, converter response tests | Four Go response converters; identity conversion validates and preserves exact bytes | Complete production 4x4 buffered generation matrix uses the request Pipeline with explicit actual protocol before source rendering | integrated | Service-owned usage/cache reconciliation may update structured metadata or body only where provider billing policy requires it. Unknown vendor fields remain preserved on identity paths. | 4x4 response matrix; native/fallback/provider tests |
| Request-scoped stream processor | `pipeline.py:StreamProcessor`, four stream suites | `protocolconv.StreamSession`, option-aware factories, lifecycle context, and byte-preserving identity mode | Complete production 4x4 HTTP/SSE generation matrix uses request-scoped stream state; Bedrock first adapts AWS EventStream to standard Anthropic events | integrated | WebSocket connection/session state and Antigravity vendor streams remain explicit transport adapters; ordinary HTTP/SSE conversion state is request-local. | 4x4 stream matrix; isolation/race; provider stream tests |
| Stream lifecycle validation | IR stream types and stream converter tests | `protocolconv/stream.Context`; identity sessions validate JSON and provider terminal policy | Cross-protocol sessions reject duplicate/incomplete/post-terminal events; identity provider transports enforce their standard terminal or documented compatible EOF policy | integrated | Responses accepts recognized terminal response events/`[DONE]`; Anthropic accepts upstream `[DONE]` only as compatibility terminal but never emits it; native Google retains documented EOF compatibility. | Lifecycle, premature EOF, malformed, terminal, disconnect tests |
| Structured non-stream upstream result | `gateway/transport/_base.py:UpstreamResponse` | `protocolconv/transport.Response` carries status, headers, body, actual protocol, IDs, timing, metadata, and ownership validation | All ordinary buffered standard-generation successes and HTTP errors across OpenAI, generic Anthropic/Bedrock, Gemini, and OpenCode Go cross this contract before conversion/rendering or service error policy | integrated | Internally streamed unary responses use bounded aggregation and preserve raw transport metadata. Image/media, compact, WebSocket, and Antigravity vendor operations are explicit exclusions. | Contract, ownership, malformed-success, provider attempt tests |
| Structured stream upstream result | `gateway/transport/_base.py:UpstreamStream` | `protocolconv/transport.Stream` exposes status, headers, actual protocol, bounded parsed records or transferred error body, and deterministic close | All ordinary HTTP/SSE standard-generation paths expose this result before source conversion/rendering; Bedrock adapts bounded AWS frames into standard Anthropic records first | integrated | Service orchestration remains near rendering to own timeout, keepalive, disconnect drain, usage, retry, and billing. This is not ResponseWriter interception. Specialized transports are excluded below. | Immediate-error, ownership, close/cancel, disconnect, provider tests |
| Bounded SSE parsing | vendored SSE client, `gateway/transport/http/transport.py` | `protocolconv/transport.SSEParser` handles LF/CRLF, comments, fields, split/multiple records, `[DONE]`, strict JSON, raw error records, bounds, cancellation, close, and race-safe progress | Every ordinary HTTP/SSE standard-generation stream; event transforms only unwrap provider transport envelopes and preserve detached raw metadata | integrated | Bedrock AWS EventStream, Responses WebSocket, compact/image/media, and Antigravity vendor parsing use reviewed specialized adapters. | Parser tables, malformed/oversized/premature terminal, fuzz, race |
| Source-specific SSE formatting | `gateway/transport/sse_format.py` | `protocolconv.Renderer` implements four framing, keepalive, terminal, and post-commit error policies | All ordinary standard-generation JSON/SSE success and synthesized error paths use the source renderer after structured provider execution | integrated | Anthropic never emits OpenAI `[DONE]`; Responses keeps `response.failed`; specialized endpoint transports retain their own framing. | Renderer wire tests and production route tests |
| Source-specific error envelopes | `gateway/proxy.py:error_response_for_source` | `protocolconv.Renderer` builds four source envelopes; service owns status/public policy and raw-upstream passthrough decisions | Standard admission, conversion, dependency, cyber, failover, malformed-success, and post-commit errors use source formatting; raw upstream error bytes remain service-owned where compatibility policy requires them | integrated | Policy ownership is intentionally outside conversion. Specialized vendor/media errors remain endpoint-owned. | Four envelope, immediate/post-commit, raw passthrough tests |
| Immediate stream error before 200 | `gateway/proxy.py:handle_streaming` | `transport.Stream.ErrorBody` transfers bounded HTTP error ownership before parser/renderer creation | Every ordinary standard HTTP/SSE generation route checks immediate errors, applies retry/health/failover policy, and only then commits source stream headers | integrated | Specialized WebSocket handshakes, image/media, compact, and Antigravity vendor operations follow their own pre-commit policy. | 4xx/5xx pre-commit, failover, ownership, header-isolation tests |
| Provider metadata preservation | `ProviderMetadataStore`, metadata round-trip tests | IR signature fields plus `protocolconv/metadata.Store`, a bounded TTL LRU keyed by tenant/API key/group/account/protocol/tool call; Pipeline restores only missing metadata and caches only the matching explicit actual protocol | Gemini compatibility Chat, Responses, and Messages plus Antigravity OAuth Chat/Responses Pipelines use service-owned Stores for Google `thoughtSignature` replay across buffered and streaming tool turns after handlers bind authenticated tenant/API-key/group identity and account selection completes | integrated | Complete scope is mandatory; missing scope disables the bridge. Account/protocol switches deliberately miss, client-provided signatures are never overwritten, and request-scoped tool routing remains Pipeline-owned. Native Google identity/`countTokens`, Gemini sticky/digest scheduling, Code Assist envelopes, and Antigravity signature rectification remain separate service policy. | Two-request buffered/streaming replay, validation, non-overwrite, tenant/API-key/group/account/protocol/failover isolation, TTL/LRU, detached-copy, concurrency, and race tests |
| Capabilities and loss policy | `capabilities.py`, base helpers, converter tests | Explicit per-protocol capability sets, typed strict errors/warnings, and opt-in `LossWarn` | All cross-protocol Pipelines default to strict loss; provider policy is named and runs outside semantic conversion | integrated | Lossy normalizations emit warnings; silent drop is prohibited. Provider-specific model/tool policy is not a protocol capability. | Capability fixtures, strict/warn, 4x4 and multipart tests |
| Shim/provider transforms | `shims/`, shim and transform tests | Named Go service policies and vendor adapters after standard conversion | Codex, Grok, Gemini/Code Assist, Bedrock, Antigravity, and OpenCode Go | integrated | A generic YAML shim runtime is excluded because sub2api already owns typed provider policy. Vendor envelopes never enter standard IR. | Per-policy and non-leak regression tests |
| Tool-call/result correlation | base tool helpers and converter tests | `ir.LinkToolResults`, request/stream state, immutable tool routes, scoped metadata, and multipart tool-result content | All 4x4 routes preserve IDs/names where representable. Responses uses native multipart output arrays; Chat uses reversible tagged dual encoding; Google uses Struct metadata plus native media parts; Anthropic uses native content blocks | integrated | Unsupported file/audio/media variants obey strict/warn policy. Vendor-only missing-ID repair remains request-scoped in the owning adapter. | Extended tools, multipart round trips, collisions, second turns, isolation/race, live tools |
| Usage and cache semantics | stream usage tests and converter suites | Common IR usage plus provider-specific extraction and existing billing services | Every standard provider route reports usage from actual upstream protocol while billing remains with the executing provider service | integrated | Keeping billing outside `protocolconv` is required ownership, not a parity gap. Local estimates and non-generation endpoints are excluded. | Usage/cache fixtures, disconnect drain, billing dedup/provider tests and live observations |
| Routing metadata | `routing.py:ResolvedRoute` | `protocolconv.Route` carries source, intended target, client/upstream models, provider, and account ID; the request pipeline consumes it directly; successful standard-generation `ForwardResult` values carry explicit actual upstream protocol | Production cross-protocol services construct explicit routes after account and model resolution, and successful provider results preserve the actual Chat, Responses, Anthropic, or Google wire schema through fallback | integrated | Scheduler and account policy remain service-owned. Non-generation `countTokens`, embedding/image/media, local emulation, and vendor-only Antigravity results are outside this result contract and may leave the protocol empty. | Route validation, provider result, and fallback actual-protocol tests |
| Gateway admin, config UI, key rotation, metrics, persistence | `gateway/admin`, config/auth/observability tests | Existing sub2api admin, repositories, scheduler, ops, billing | Existing product domains | excluded | sub2api already owns these product capabilities. Rewriting the reference standalone gateway administration would duplicate unrelated systems. Only protocol fields needed by existing ops are applicable. | Existing subsystem tests |
| Embeddings and non-generation passthrough | `gateway/embeddings.py`, `send_passthrough` | Existing OpenAI embedding/media services | Dedicated non-standard routes | excluded | The 4x4 standard generation architecture does not cover embeddings, image/video endpoints, compact, or arbitrary passthrough. Shared transport primitives may be reused, but no IR conversion is required. | Existing endpoint tests |
| Automatic protocol detection | `auto_detect.py` | Deliberately absent from core | None | excluded | Production source protocol is defined by the route. Target/actual protocols come from routing and account capability. Guessing from body, endpoint, or model is prohibited. | Negative inference tests |
| Python runtime and SDK compatibility layer | public Python API and SDK examples | None | None | excluded | Python is a reference/test oracle only. Production remains Go. SDK behavior is verified through HTTP wire compatibility and probes. | Go tests and live probes |

## Production route inventory

The table describes the current branch after the ordinary standard-generation refactor. Provider execution returns structured protocol results to a source renderer inside the service orchestration layer; keeping that orchestration near timeout, disconnect, usage, and billing state is intentional and does not intercept or replace `gin.ResponseWriter`.

| Ingress route | Source protocol | Current selection and target behavior | Actual upstream protocol possibilities | Current renderer and usage owner | Required integration action |
| --- | --- | --- | --- | --- | --- |
| `/v1/chat/completions`, `/chat/completions` | OpenAI Chat | Route dispatches by group platform to OpenAI gateway, OpenCode Go, or generic Gateway; selected Antigravity OAuth/setup-token accounts enter the standard-to-vendor bridge | Chat, Responses, Anthropic, or Google depending on account path; vendor-specific Grok and Antigravity transports also exist | Ordinary Chat-to-Responses, raw Chat same-protocol, generic Chat-to-Anthropic, Gemini compatibility Chat-to-Google, and Antigravity OAuth Chat-to-Google buffered/streaming successes use one request pipeline, explicit actual protocol, bounded stream parsing, and Chat renderer. Antigravity then applies model-family policy and the `v1internal` envelope before its existing OAuth/retry/rate-limit transport; API-key and upstream relay accounts retain the generic path. Selected Bedrock accounts run the converted Anthropic body through native model resolution, beta/CC policy, SigV4 or Bedrock API-key auth, bounded AWS EventStream decoding, then the same actual-Anthropic Pipeline/renderer path. Buffered and streaming Chat-over-Responses share the structured bounded transport while silent-refusal, `response.failed`, usage, and disconnect policy stay service-owned; actual Responses/raw Chat HTTP errors are bounded structured results before Chat source policy and stream commitment; raw Chat keeps provider transforms and preserves an empty buffered 2xx body as an explicit legacy exception; OAuth Google wrappers are transport transforms; Cursor Responses-shaped compatibility is the explicit response-only converter exception | Retain Cursor and Grok-specific request policy; keep Antigravity authentication, family transforms, envelope, retry, scheduling, and billing outside standard IR. |
| `/v1/responses`, `/responses`, subpaths | OpenAI Responses | Route dispatches by group platform; OpenCode Go is rejected; selected Antigravity OAuth/setup-token accounts enter the standard-to-vendor bridge | Responses, OpenAI Chat fallback, Anthropic, or Google according to the selected account | Chat fallback, generic Anthropic target including selected Bedrock accounts, Gemini compatibility Google target, Antigravity OAuth Responses-to-Google, and ordinary native plus passthrough Responses JSON/stream successes use explicit actual-protocol results, one request pipeline per actual HTTP attempt, bounded stream parsing, and the Responses renderer. Antigravity applies model-family policy and the `v1internal` envelope outside IR, then retains its OAuth/retry/rate-limit/usage/billing ownership. Bedrock executes only after Responses-to-Anthropic conversion and adapts bounded AWS EventStream records back to standard Anthropic events before response conversion. Passthrough also structures explicit SSE-as-unary success. Ordinary native provider-core, native passthrough, Grok main generation, and Responses-to-raw-Chat HTTP errors carry explicit actual protocol through bounded structured results; stream-request error bodies transfer and close before retry/failover/source policy or SSE commitment. Native success streaming preserves timeout/keepalive, pre-output failover, usage/cyber policy, client disconnect draining, client-model restoration, and route-appropriate response headers | Retain Grok composer/image/media, `/responses/compact`, content-type/body-heuristic aggregation, Responses WebSocket state, and Antigravity vendor transport as specialized adapters. |
| `/v1/messages` | Anthropic Messages | Route dispatches among OpenAI, OpenCode Go, and generic Gateway | Anthropic, Responses, Chat fallback, Google, Antigravity vendor path | OpenAI raw-Chat fallback, OpenAI Responses target, Gemini compatibility Google target, generic native Anthropic, and Anthropic API-key native passthrough successes use request pipelines and the Anthropic renderer; buffered and streaming Messages-over-Responses use structured bounded streams through the request Pipeline while timeout, keepalive, `response.failed`, usage, and disconnect policy stay service-owned; actual Responses/raw Chat HTTP errors are bounded structured results before continuation/failover/Anthropic source policy and stream commitment; generic native Anthropic and API-key passthrough create one identity Pipeline per actual upstream attempt after final standard body sanitization, with generic buffered output crossing `transport.Response` and streaming output crossing `transport.Stream` while preserving cache/model/tool, noop keepalive, failover, timeout, and disconnect-drain policy. Both native paths collect each HTTP error once with actual Anthropic protocol; stream-request errors transfer through `ErrorBody`, and retry/failover/source policy consumes the final detached body and headers before SSE commitment | Retain the documented Codex Messages provider policy and Antigravity vendor adapter as classified boundaries. |
| `/v1beta/models/{model}:generateContent` | Google GenAI | Google auth middleware plus group assignment; OpenAI and Anthropic groups enter one standard-provider account loop while Gemini and forced Antigravity retain native policy | Google AI Studio/Code Assist/Vertex, Antigravity, OpenAI Responses/Codex/raw Chat fallback, or Anthropic Messages according to account policy | Native Gemini creates one Google identity Pipeline per actual generation attempt after signature policy and before Code Assist wrapping; unary and Code Assist SSE-as-unary successes cross `transport.Response` and the Google renderer with raw wrapper bytes retained as metadata. OpenAI and Anthropic targets share scheduling/failover/concurrency admission but retain provider-specific execution and billing, both using one Google-source request pipeline and renderer; selected Bedrock accounts execute after Google-to-Anthropic conversion through the native AWS adapter and return actual-Anthropic events to the same Google Pipeline | Keep Gemini signature/digest, `countTokens`, and Antigravity vendor policy on native paths. |
| `/v1beta/models/{model}:streamGenerateContent` | Google GenAI | Same admission and account loop as non-stream | Google/Antigravity native stream, OpenAI Responses/raw Chat fallback stream, or Anthropic Messages stream | OpenAI Responses, including passthrough accounts, and Anthropic paths convert explicit actual-protocol events through request-scoped Google Pipelines and renderers with bounded structured streams; Responses policy retains pre-output failover, usage/cyber handling, disconnect draining, and excludes native-only `x-codex-*` headers. Native Gemini uses one identity Pipeline per actual attempt, bounded parsing, Code Assist transport transforms, and the Google renderer; EOF/`[DONE]` remain compatible terminals | Keep Antigravity's specialized stream policy separate and audit only genuinely standard generation boundaries. |
| `/v1beta/models`, `/v1beta/models/{model}` | Google model discovery | Gemini and forced Antigravity retain existing behavior; OpenAI/Anthropic groups render `GetAvailableModels` aliases in Google schema | Gemini model endpoint, Antigravity mapping, or generation-routable standard-provider aliases | Standard-provider listings exclude wildcard patterns and mapping targets, Anthropic empty mappings use its routable default surface, and GetModel returns 404 outside the same surface | Add another platform only with matching generation tests; never expose discovery-only aliases. |
| `/v1/models`, aliases | OpenAI model discovery | Group-platform-specific model handlers and manifests | Group/account model mappings | Handler-specific schemas | Separate Codex manifest compatibility from standard OpenAI model listing; list only generation-routable models. |
| OpenCode Go `/v1/chat/completions` path | OpenAI Chat | Account `model_protocols` selects Chat or Messages | Chat or Anthropic | Same- and cross-protocol request, response, and stream successes use one request-scoped Pipeline, explicit actual protocol, bounded parser, structured result, and Chat renderer; identity paths preserve exact upstream JSON/event bytes and require `[DONE]` before renderer terminal output. Bounded non-stream HTTP errors cross `transport.Response`; stream-request errors transfer through `transport.Stream.ErrorBody`, both with explicit actual protocol before raw passthrough/failover policy and stream commitment | Retain stream-usage injection and the post-conversion OpenCode cache-anchor provider transform. |
| OpenCode Go `/v1/messages` path | Anthropic Messages | Account `model_protocols` selects Messages or Chat | Anthropic or Chat | Same- and cross-protocol request, response, and stream successes use one request-scoped Pipeline, explicit actual protocol, bounded parser, structured result, and Anthropic renderer; identity paths preserve exact upstream JSON/event bytes and require `message_stop`. Bounded non-stream HTTP errors cross `transport.Response`; stream-request errors transfer through `transport.Stream.ErrorBody`, both with explicit actual protocol before raw passthrough/failover policy and stream commitment | Retain OpenCode cache-anchor request policy as a named provider transform after standard conversion. |
| Responses WebSocket and HTTP fallback | OpenAI Responses | OpenAI transport resolver selects WS or HTTP/SSE | Responses over WS or HTTP/SSE | Dedicated WS forwarders and HTTP bridge | Keep connection/session mechanics as transport adapters. Feed parsed Responses events into the common processor when crossing protocols. |
| `/antigravity/v1/*`, `/antigravity/v1beta/*` | Anthropic or Google | Forced Antigravity account selection | Antigravity `v1internal` vendor envelope | Antigravity adapter/service | Retain vendor boundary. Standard conversion may run before/after the adapter, but vendor schema and signature repair never enter standard converters. |

## Legacy bridge classification

| Existing bridge or policy | Classification | Reason and owner |
| --- | --- | --- |
| `internal/pkg/apicompat` pairwise helpers | retain only at classified boundaries | Ordinary production generation no longer calls pairwise request converters. Remaining production calls are the Cursor Responses-shaped `/chat/completions` response-only bridge and non-generation `countTokens`; `apicompat` wire structs/accumulators may still support provider policy without owning standard conversion. |
| OpenAI Messages to Codex Responses policy | retain as named provider transform | The normalized Anthropic request is converted exactly once by the standard Pipeline. The retained transform mutates only the resulting Responses body for developer-input placement, billing-header filtering, continuation/todo guards, minimum output budget, provider defaults/schema normalization, and OAuth transport policy. Standard messages, tools, call IDs, and multipart tool results remain Pipeline-owned. Response JSON and stream semantics use that same request Pipeline. Owner: OpenAI/Codex service. |
| OpenAI Responses to Chat fallback transport | retain transport fallback, replace semantic conversion | Endpoint selection/retry remains OpenAI service policy. Request, non-stream response, and stream semantics use one request-scoped pipeline with actual upstream `openai_chat_completions`; usage extraction and billing remain service-owned. |
| Gemini Chat, Responses, and Messages compatibility services | replace standard semantics; retain account/auth/envelope policy | AI Studio versus OAuth/Code Assist URL/auth and retry behavior remain provider-owned. All three compatibility formats now use request pipelines and source renderers through one shared Google provider executor. A complete authenticated tenant/API-key/group plus selected-account scope enables bounded Google `thoughtSignature` replay; native Gemini identity, sticky/digest scheduling, and `countTokens` remain separate. |
| OpenCode Go cache anchor injection | retain as provider transform | It is an upstream caching policy, not a standard Anthropic semantic requirement. Owner: OpenCode Go service. |
| Antigravity converters, envelope, signature repair, call-ID behavior | retain vendor adapter | The upstream is not a standard Google endpoint and may omit IDs or require vendor signatures. |
| Grok/xAI composer and media behavior | retain provider adapter | Provider/media behavior is not representable as generic four-protocol generation semantics. |
| OpenAI compact, image generation, and WebSocket state | retain specialized paths | These are endpoint/transport capabilities outside the standard 4x4 generation matrix. Shared parser/result contracts may be reused. |

## Residual compatibility branch classification

| Branch | Production reachability | Classification and evidence |
| --- | --- | --- |
| `openai_gateway_chat_completions.go` with `pipeline == nil` | Only a body with `input` and no `messages` sent to `/chat/completions` reaches it | Intentional Cursor Responses-shaped endpoint compatibility. The request is already Responses wire format, so no Chat-source request Pipeline exists; buffered and streaming replies retain the characterized response-only Responses-to-Chat bridge. Ordinary Chat requests always construct a Pipeline. |
| Native OpenAI Responses with `output == nil` | Public native Responses calls begin nil, but every non-compact HTTP attempt constructs a fresh Responses identity Pipeline/output before request execution | Integrated standard generation. Retry attempts rebuild isolated output state. The lower `nil` writer branches remain reachable only to direct tests, Grok native execution, and compact. |
| OpenAI Responses passthrough with `output == nil` | Public passthrough calls begin nil; every non-compact call upgrades it to an identity Pipeline/output before upstream execution | Integrated standard generation. `/responses/compact` intentionally remains the scanner/aggregation exception. |
| Grok main Responses generation with `output == nil` | Reachable only from native OpenAI Responses routing; Google-to-OpenAI requires an account whose platform is exactly `openai` | Retained provider adapter. It receives and returns Responses schema, uses bounded structured HTTP errors and explicit actual protocol, and preserves xAI quota/account policy. Composer image bridging and media endpoints remain non-standard provider capabilities. |
| `handleSSEToJSON`/legacy native Responses wrappers | No ordinary OpenAI production caller after identity-output integration; direct calls are tests, while Grok and compact enter through their explicit wrappers | Retained implementation detail for characterized Grok/compact SSE-as-unary aggregation, not a standard 4x4 gap. |
| `apicompat.AnthropicToResponses` in `openai_gateway_count_tokens.go` | Native non-generation token estimate only | Explicit non-generation exclusion. It supplies a local/tokenizer working shape and does not execute a generation request. |

## Production 4x4 evidence

Each cell below has buffered and streaming conversion coverage plus explicit actual-protocol assertions in the listed production service tests. Authentication, endpoint selection, retry/failover, malformed-success handling, disconnect draining, and usage remain covered by the provider-specific suites named in the route inventory.

| Source | Chat upstream | Responses upstream | Anthropic upstream | Google upstream |
| --- | --- | --- | --- | --- |
| OpenAI Chat | raw Chat identity and OpenCode Go identity tests | `ForwardAsChatCompletions_*` and Chat Pipeline tests | `GatewayService_ForwardAsChatCompletions` plus Bedrock tests | `GeminiForwardAsChatCompletions_*` |
| OpenAI Responses | Responses Chat fallback tests | native/passthrough Responses identity tests | `GatewayService_ForwardAsResponses` plus Bedrock tests | `GeminiForwardAsResponses_*` |
| Anthropic Messages | Messages Chat fallback and OpenCode Go Messages-to-Chat tests | `ForwardAsAnthropic_*` Pipeline/Codex tests | generic/API-key Anthropic identity tests | `GeminiMessagesCompatServiceForward_*` |
| Google GenAI | `ForwardGoogleGenAIStreamingRawChatFallbackUsesActualProtocol` | `ForwardGoogleGenAI*ResponsesPipeline` | generic/Bedrock Google-to-Anthropic tests | native Gemini identity Pipeline tests |

Package-level `TestRequestMatrixPreservesCoreSemantics`, `TestResponseMatrixPreservesCoreSemantics`, and `TestStreamMatrixPreservesLifecycleAndSemantics` independently exercise all 16 semantic cells. Multipart tool results are additionally verified from Anthropic to Responses, Chat dual encoding, and Google Struct/native-media encoding without strict-mode loss.

## Current completion gates

- On 2026-07-14, backend, unit-tag, frontend build/typecheck/lint, vet, protocol conversion race, two 20-second fuzz targets, formatting, diff, and secret-scan gates passed on the current implementation HEAD.
- The Plan's broad service race expression passes with `-parallel=1`. At default parallelism, pre-existing tests in 64 files concurrently call Gin's package-global `SetMode`, producing a test-infrastructure race unrelated to request conversion; focused protocol/provider race suites pass without serializing tests.
- Probe binaries rebuilt from `25eeeef2` exercised six actual wire paths, four source protocols, and buffered/streaming modes: 48/48 text cases ultimately returned HTTP 200 with zero conversion warnings. All six two-request tool flows returned 200/200 with preserved correlation, final text, usage, and zero warnings. An anonymous candidate window had 61/61 token-complete, positive-cost usage rows and 61/61 billing-dedup matches.
- The production gateway remained on `main@dc6ae080`; probes therefore validate branch converter behavior against real wire plus the existing provider billing loop, not deployment of the branch handlers. Automated production-route coverage and the direct-wire probes satisfy the branch's merge-preparation gates. Deployment and production-handler live acceptance remain the separate post-merge release phase defined by the Plan. The Plan remains present until the merge decision is completed.
