# LLM-Rosetta Go Rewrite Gap Audit

## Scope and baseline

- sub2api baseline: `main@dc6ae080`
- implementation branch: `feature/llm-rosetta-go-rewrite`
- reference repository: `../llm-rosetta-reference`
- pinned reference commit: `81ed15999e9a9c40082a7392bd0c0366c18960a0`
- audited protocols: OpenAI Chat Completions, OpenAI Responses, Anthropic Messages, Google GenAI

This audit tracks behavioral responsibilities, not file-for-file translation. sub2api keeps account selection, authentication, retries, sticky sessions, provider policy, usage accounting, and billing in its existing service domains. The protocol conversion package owns only standard wire semantics and request-scoped conversion state.

Status values used below:

- **integrated**: implemented and used by all applicable production paths.
- **isolated**: implemented in Go and tested, but not consistently used by production paths.
- **partial**: only part of the reference behavior or route matrix is implemented.
- **legacy duplicate**: production behavior exists in service-local or pairwise code outside the common core.
- **missing**: no applicable Go implementation.
- **excluded**: intentionally not part of this rewrite, with the reason recorded.

## Responsibility ledger

| Reference responsibility | Reference files and tests | Current Go implementation | Production entrypoints using it | Status | Known difference and required action | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| Explicit protocol IDs | `auto_detect.py`, `routing.py`, `tests/test_auto_detect.py` | `internal/pkg/protocolconv/protocol.go` | Services call explicit protocol IDs in selected compatibility paths | partial | Core correctly refuses endpoint/model inference. Production admission still chooses handlers from group platform. Add an explicit source/intended-target/actual-upstream route contract. Auto-detection is excluded from production core. | Protocol parsing tests; production route tests |
| Converter registry | `converters/__init__.py`, `tests/test_public_api.py` | `protocolconv.Registry`, `standard.NewRegistry` | Anthropic compatibility, Gemini compatibility, OpenCode Go, Antigravity adapters | isolated | One registry exists, but production coverage is selective. Route every standard cross-protocol path through it. | Registry tests; service integration matrix |
| One-shot conversion pipeline | `pipeline.py`, `tests/test_pipeline.py`, `tests/test_pipeline_profile.py` | `protocolconv.Pipeline` binds explicit protocols, model identities, warnings, response conversion, and stream creation | OpenAI Responses-to-Chat and Anthropic Messages-to-Chat/Responses paths, generic Gateway Chat/Responses-to-Anthropic, and Gemini compatibility Chat/Responses/Messages-to-Google preserve one pipeline through request, response, and stream conversion; retained Codex request policy runs after the standard request lifecycle | partial | Extend the same request pipeline through remaining provider response and stream renderers. | Pipeline lifecycle, actual-protocol, and provider integration tests |
| Request to IR to target request | `pipeline.py`, four converter suites | Four Go converters and common IR | OpenAI Responses-to-Chat, Anthropic Messages-to-Chat, ordinary Chat-to-Responses, OpenCode Go cross-protocol, Google GenAI-to-OpenAI Responses, and Gemini compatibility Chat/Responses/Messages-to-Google use request-scoped pipelines; selected native Gemini and Antigravity paths use shared converters without the full route lifecycle | partial | Extend the same pipeline instance across remaining provider request/response paths. Replace pairwise production bridges where no vendor policy is involved. | 4x4 core and production request matrices |
| Target response to IR to source response | `pipeline.py`, converter response tests | Four Go response converters | OpenAI Responses-to-Chat, Anthropic Messages-to-Chat/Responses, ordinary Chat-to-Responses, OpenCode Go cross-protocol, generic Anthropic targets, and Gemini compatibility Chat/Responses/Messages-to-Google non-stream successes convert with explicit actual protocols through their request pipelines; other compatibility paths remain mixed | partial | Extend explicit actual-protocol response conversion across providers and remove branch/account inference. | 4x4 response matrix and fallback tests |
| Request-scoped stream processor | `pipeline.py:StreamProcessor`, four stream suites | `protocolconv.StreamSession`, option-aware stream factories, lifecycle context | OpenAI Responses-to-Chat, Anthropic Messages-to-Chat, OpenCode Go cross-protocol, generic Gateway Chat/Responses-to-Anthropic, and Gemini compatibility Chat/Responses/Messages-to-Google paths create stream processors from their request pipelines and structured actual protocols | partial | Integrate remaining standard streams. Keep request-scoped model/tool metadata immutable and vendor policy outside the core. | 4x4 stream matrix; extended-tool isolation and race tests |
| Stream lifecycle validation | IR stream types and stream converter tests | `protocolconv/stream.Context` | Only paths using `StreamSession` | partial | Duplicate starts, incomplete blocks, post-terminal data, and missing terminal are validated in the core. Legacy service scanners use separate terminal rules. Move standard paths to the common processor. | Lifecycle unit tests; premature EOF route tests |
| Structured non-stream upstream result | `gateway/transport/_base.py:UpstreamResponse` | `protocolconv/transport.Response` carries status, headers, body, actual protocol, IDs, timing, and metadata with validation | OpenAI Responses-to-Chat fallback, Chat/Messages-over-Responses buffered success, native Responses JSON, raw Chat JSON, and Gemini compatibility Chat/Responses/Messages-to-Google success paths collect raw upstream bodies, filtered headers, IDs, usage, and explicit actual protocols before compatibility rendering; buffered Responses collectors preserve unknown terminal fields while supplementing sparse output/usage | partial | Extend the contract across remaining Responses SSE-as-unary, errors, and providers. Provider cores must preserve raw upstream bytes and must not write to `gin.Context`; route timing remains owned by the outer forward result until signatures are consolidated. | Contract tests; provider refactor characterization |
| Structured stream upstream result | `gateway/transport/_base.py:UpstreamStream` | `protocolconv/transport.Stream` exposes status, actual protocol, parsed events/error body, and deterministic close | OpenAI Responses and Anthropic Messages Chat-fallback, OpenCode Go cross-protocol, generic Gateway Chat/Responses-to-Anthropic, and Gemini compatibility Chat/Responses/Messages-to-Google success streams expose structured actual-protocol results before downstream conversion | partial | Extend to native Responses, raw Chat, Chat-over-Responses, errors, and remaining providers. Downstream status is still committed inside many service methods. | Immediate-error, ownership, close/cancel, and provider characterization tests |
| Bounded SSE parsing | vendored SSE client, `gateway/transport/http/transport.py` | `protocolconv/transport.SSEParser` covers bounded records, SSE fields, CRLF, split reads, `[DONE]`, JSON validation, cancellation, and close; request-scoped event transforms unwrap vendor transport envelopes before protocol decoding; legacy scanners remain | OpenAI Responses and Anthropic Messages Chat-fallback, OpenCode Go cross-protocol, generic Gateway Chat/Responses-to-Anthropic, and Gemini compatibility Chat/Responses/Messages-to-Google streams use the common parser with configured record bounds and deterministic body ownership | partial | Migrate remaining standard provider streams and leave specialized image/compact/WebSocket parsing only where transport semantics differ. | Parser table tests, malformed/premature terminal route tests, and fuzzing |
| Source-specific SSE formatting | `gateway/transport/sse_format.py` | `protocolconv.Renderer` implements four framing policies and OpenAI Chat/Responses `[DONE]`; legacy formatting remains | OpenAI Responses-to-Chat and Anthropic Messages-to-Chat fallbacks, generic Gateway Chat/Responses-to-Anthropic, and Gemini compatibility Chat/Responses/Messages-to-Google use their source renderers for JSON and SSE generation success paths | partial | Move remaining JSON handlers and streams to the renderer after provider services return structured results. | Wire-format renderer and production fallback tests |
| Source-specific error envelopes | `gateway/proxy.py:error_response_for_source` | `protocolconv.Renderer` builds four source envelopes; handler-local writers remain | Gemini compatibility Responses-to-Google uses the Responses renderer for local conversion and mapped provider errors; older routes retain local writers for wire compatibility | partial | Preserve status/message policy in handlers/services while replacing schema construction with the source renderer one characterized route at a time. | Four envelope tests; admission/upstream error tests |
| Immediate stream error before 200 | `gateway/proxy.py:handle_streaming` | Varies by provider path | Some paths inspect `resp.StatusCode`; others commit headers early | partial | Common structured stream result is required before renderer commitment. | Upstream 4xx/5xx stream route tests |
| Provider metadata preservation | `ProviderMetadataStore`, metadata round-trip tests | IR signature fields plus `protocolconv/metadata.Store`, a bounded TTL LRU keyed by tenant/API key/group/account/protocol/tool call; Gemini sticky/digest and Antigravity policies remain service-owned | Common store is not yet wired into provider services; request-scoped extended-tool routing metadata is the next pipeline integration prerequisite | isolated | The store deliberately misses after account or protocol switches and cannot be used without a complete tenant scope. Keep per-request tool routing in the pipeline, not this cross-request store. Integrate signatures with existing sticky/digest ownership during Google/Anthropic refactors; Antigravity repair stays vendor-owned. | Collision, TTL/LRU, failover, account-switch, detached-copy, request isolation, and race tests |
| Capabilities and loss policy | `capabilities.py`, base helpers, converter tests | `capability.go`, typed warnings, strict/warn policy | Strict conversion helpers in selected production paths | isolated | Vision/reasoning/provider transforms remain service-local. Inventory each retained transform and make warning/drop opt-in. | Capability fixtures; strict/warn tests |
| Shim/provider transforms | `shims/`, shim and transform tests | Provider policy spread across service packages | OpenAI, Grok, Gemini, Antigravity, OpenCode paths | partial | A generic YAML shim runtime is not required. Applicable transforms should be named Go policies after standard conversion. Vendor envelopes remain adapters. | Per-policy regression tests |
| Tool-call/result correlation | base tool helpers and converter tests | `ir.LinkToolResults`, converter state, stream state; IR/Responses conversion preserve function/custom/tool-search kinds, namespace trees/calls, and scalar/object/array results; Pipeline owns immutable target-name restoration metadata and passes it to option-aware stream factories | OpenAI Responses-to-Chat and generic Gateway Responses-to-Anthropic non-stream and stream production paths use pipeline restoration; Anthropic targets lower custom/tool-search and flatten namespace tools deterministically | partial | Extend the same metadata path to other standard protocol fallbacks. Keep vendor call-ID repair only where standard IR cannot represent upstream behavior. | Extended tool round trips, scalar/object/array results, target-name collision, request isolation/race, stream, and second-turn matrix |
| Usage and cache semantics | stream usage tests and converter suites | Common IR usage plus service-specific extraction | All services, independently | partial | Conversion preserves normalized usage, but billing still reads provider-local structures. During provider refactors, return provider usage metadata alongside structured results and preserve billing ownership. | Usage/cache fixtures; billing dedup tests |
| Routing metadata | `routing.py:ResolvedRoute` | `protocolconv.Route` carries source, intended target, client/upstream models, provider, and account ID; the request pipeline consumes it directly | Production cross-protocol services construct explicit routes after account and model resolution; the former service-local one-shot conversion helpers have been removed | partial | Extend provider forwarding results with explicit actual protocol without moving scheduler policy into `protocolconv`. | Route validation, resolution, and fallback tests |
| Gateway admin, config UI, key rotation, metrics, persistence | `gateway/admin`, config/auth/observability tests | Existing sub2api admin, repositories, scheduler, ops, billing | Existing product domains | excluded | sub2api already owns these product capabilities. Rewriting the reference standalone gateway administration would duplicate unrelated systems. Only protocol fields needed by existing ops are applicable. | Existing subsystem tests |
| Embeddings and non-generation passthrough | `gateway/embeddings.py`, `send_passthrough` | Existing OpenAI embedding/media services | Dedicated non-standard routes | excluded | The 4x4 standard generation architecture does not cover embeddings, image/video endpoints, compact, or arbitrary passthrough. Shared transport primitives may be reused, but no IR conversion is required. | Existing endpoint tests |
| Automatic protocol detection | `auto_detect.py` | Deliberately absent from core | None | excluded | Production source protocol is defined by the route. Target/actual protocols come from routing and account capability. Guessing from body, endpoint, or model is prohibited. | Negative inference tests |
| Python runtime and SDK compatibility layer | public Python API and SDK examples | None | None | excluded | Python is a reference/test oracle only. Production remains Go. SDK behavior is verified through HTTP wire compatibility and probes. | Go tests and live probes |

## Production route inventory

The table describes the current branch before the full provider refactor. "Renderer" names the code currently writing the downstream response, not the desired architecture.

| Ingress route | Source protocol | Current selection and target behavior | Actual upstream protocol possibilities | Current renderer and usage owner | Required integration action |
| --- | --- | --- | --- | --- | --- |
| `/v1/chat/completions`, `/chat/completions` | OpenAI Chat | Route dispatches by group platform to OpenAI gateway, OpenCode Go, or generic Gateway | Chat, Responses, Anthropic, Google depending on account path; vendor-specific Grok/Antigravity branches also exist | Ordinary Chat-to-Responses, generic Chat-to-Anthropic, and Gemini compatibility Chat-to-Google buffered/streaming successes use one request pipeline, structured actual protocol, bounded stream parsing, and Chat renderer; OAuth Google wrappers are transport transforms; retry, signature injection, usage, and provider policy remain service-owned; Cursor Responses-shaped compatibility is the explicit response-only exception | Structure remaining raw Chat/native provider results and retain Cursor, Grok, and Antigravity exceptions with characterization tests. |
| `/v1/responses`, `/responses`, subpaths | OpenAI Responses | Route dispatches by group platform; OpenCode Go is rejected | Responses, OpenAI Chat fallback, Anthropic, or Google according to the selected account | Chat fallback, generic Anthropic target, and Gemini compatibility Google target non-stream/stream successes use structured actual-protocol results, one request pipeline per attempt, bounded stream parsing, and the Responses renderer; the Google cell reuses the same Gemini auth/retry/failover/usage executor as Chat ingress; native Responses JSON is structured but retains media-type/model/compact compatibility rendering | Extend structured results to native SSE, SSE-as-unary, and errors, then consolidate compatibility policies around the renderer. Keep Responses WebSocket state as a transport adapter. |
| `/v1/messages` | Anthropic Messages | Route dispatches among OpenAI, OpenCode Go, and generic Gateway | Anthropic, Responses, Chat fallback, Google, Antigravity vendor path | OpenAI raw-Chat fallback, OpenAI Responses target, and Gemini compatibility Google target successes use request pipelines and the Anthropic renderer; Responses transport retains its scanner/timer/keepalive policy while standard response and stream semantics now flow through the request Pipeline; Gemini signature retry creates a fresh pipeline for each transformed request while retry, signature, usage, and billing policy remain service-owned | Structure the remaining native transport boundaries. Retain documented Codex Messages request policy and Antigravity adapter. |
| `/v1beta/models/{model}:generateContent` | Google GenAI | Google auth middleware plus group assignment; OpenAI groups enter a dedicated standard-protocol loop while Gemini and forced Antigravity retain native policy | Google AI Studio/Code Assist, Antigravity, OpenAI Responses/Codex, passthrough Responses, or raw Chat fallback according to account policy | OpenAI provider core exposes explicit structured actual-protocol output before rendering; the Google route uses one request pipeline and Google renderer without replacing/intercepting `gin.ResponseWriter`, plus shared scheduling/failover/concurrency and OpenAI usage billing | Extend the same route to other standard targets only after their complete generation and billing loops exist. Keep Gemini signature/digest and Antigravity vendor policy on native paths. |
| `/v1beta/models/{model}:streamGenerateContent` | Google GenAI | Same admission and account loop as non-stream | Google/Antigravity native stream, OpenAI Responses stream, passthrough Responses stream, or raw Chat fallback stream | OpenAI provider loops emit normalized JSON events with explicit actual protocol to a request-scoped Google output; Pipeline/renderer own conversion/framing, pre-output failover remains available, and provider core still owns usage, terminal, cyber, tool correction, and client-drain policy | Migrate native Gemini stream transport boundary separately without moving signature/digest policy into the standard core. |
| `/v1beta/models`, `/v1beta/models/{model}` | Google model discovery | Gemini and forced Antigravity retain existing behavior; OpenAI groups render `GetAvailableModels` aliases in Google schema | Gemini model endpoint, Antigravity mapping, or generation-routable OpenAI account aliases | OpenAI listing excludes wildcard patterns and mapping targets, and GetModel returns 404 outside the same schedulable surface | Add another platform only with matching generation tests; never expose discovery-only aliases. |
| `/v1/models`, aliases | OpenAI model discovery | Group-platform-specific model handlers and manifests | Group/account model mappings | Handler-specific schemas | Separate Codex manifest compatibility from standard OpenAI model listing; list only generation-routable models. |
| OpenCode Go `/v1/chat/completions` path | OpenAI Chat | Account `model_protocols` selects Chat or Messages | Chat or Anthropic | Cross-protocol request, response, and stream use one pipeline with structured actual protocol and source renderer; same-protocol paths remain service-local | Structure same-protocol results and errors while retaining the post-conversion OpenCode cache-anchor provider transform. |
| OpenCode Go `/v1/messages` path | Anthropic Messages | Account `model_protocols` selects Messages or Chat | Anthropic or Chat | Cross-protocol request, response, and stream use one pipeline with structured actual protocol and source renderer; same-protocol paths remain service-local | Structure same-protocol results and errors; retain OpenCode cache-anchor request policy as a named provider transform after standard conversion. |
| Responses WebSocket and HTTP fallback | OpenAI Responses | OpenAI transport resolver selects WS or HTTP/SSE | Responses over WS or HTTP/SSE | Dedicated WS forwarders and HTTP bridge | Keep connection/session mechanics as transport adapters. Feed parsed Responses events into the common processor when crossing protocols. |
| `/antigravity/v1/*`, `/antigravity/v1beta/*` | Anthropic or Google | Forced Antigravity account selection | Antigravity `v1internal` vendor envelope | Antigravity adapter/service | Retain vendor boundary. Standard conversion may run before/after the adapter, but vendor schema and signature repair never enter standard converters. |

## Legacy bridge classification

| Existing bridge or policy | Classification | Reason and owner |
| --- | --- | --- |
| `internal/pkg/apicompat` generic Chat/Responses/Anthropic pairwise converters | replace progressively | Standard message/tool/usage semantics belong to the shared IR. Characterize compatibility before removal. |
| OpenAI Messages to Codex Responses policy | retain as named provider transform | The standard Anthropic-to-Responses request lifecycle now runs first; the retained transform controls developer ordering, continuation/todo guards, Codex call IDs, and provider defaults beyond standard schema conversion. Response JSON and stream semantics use that request Pipeline. Owner: OpenAI/Codex service. |
| OpenAI Responses to Chat fallback transport | retain transport fallback, replace semantic conversion | Endpoint selection/retry remains OpenAI service policy. Request, non-stream response, and stream semantics use one request-scoped pipeline with actual upstream `openai_chat_completions`; usage extraction and billing remain service-owned. |
| Gemini Chat, Responses, and Messages compatibility services | replace standard semantics; retain account/auth/envelope policy | AI Studio versus OAuth/Code Assist URL/auth and retry behavior remain provider-owned. All three compatibility formats now use request pipelines and source renderers through one shared Google provider executor; native Gemini policy remains separate. |
| OpenCode Go cache anchor injection | retain as provider transform | It is an upstream caching policy, not a standard Anthropic semantic requirement. Owner: OpenCode Go service. |
| Antigravity converters, envelope, signature repair, call-ID behavior | retain vendor adapter | The upstream is not a standard Google endpoint and may omit IDs or require vendor signatures. |
| Grok/xAI composer and media behavior | retain provider adapter | Provider/media behavior is not representable as generic four-protocol generation semantics. |
| OpenAI compact, image generation, and WebSocket state | retain specialized paths | These are endpoint/transport capabilities outside the standard 4x4 generation matrix. Shared parser/result contracts may be reused. |

## Highest-priority gaps

1. Source protocol admission remains coupled to group platform outside implemented cells. `/v1beta` now admits OpenAI groups with complete generation/discovery coverage, while other non-Gemini standard targets remain rejected; root aliases still retain `requireGroupAnthropic` assignment policy.
2. Provider generation methods write directly to `gin.Context`; handlers use writer size to decide failover. This prevents a common source renderer and makes immediate stream errors path-dependent.
3. Actual upstream protocol is not carried by every forward result. Fallback paths can only be interpreted by knowing which service branch ran.
4. SSE parsing and formatting are duplicated across Anthropic, OpenAI, Gemini, OpenCode Go, image, compact, and WebSocket code.
5. Package-level 4x4 matrices do not prove production account routing, authentication, endpoint selection, errors, usage, or billing.
6. Cross-request provider metadata has no reviewed tenant-safe common ownership. Existing sticky/digest behavior must be integrated before introducing any shared store.

## Implementation order and gates

1. Add request-scoped pipeline, structured transport contracts, bounded SSE parser, and source renderers under `internal/pkg/protocolconv`.
2. Add characterization tests for current route admission, fallback selection, wire output, usage, and billing.
3. Refactor one provider core at a time to return structured results: OpenAI Responses/fallback, OpenAI Chat, Anthropic, Google, OpenCode Go, then applicable WebSocket boundaries.
4. Move handlers to source renderers only after the selected provider returns status and actual protocol without writing downstream.
5. Remove platform admission gates only when generation and model discovery tests prove the corresponding source/target cells.
6. Run the 4x4 production matrix, race/fuzz tests, full backend/frontend gates, secret scan, and redacted live probes before merge.
